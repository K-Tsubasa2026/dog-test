POSTGRES_DB=dogtest
POSTGRES_USER=dogtest
POSTGRES_PASSWORD=changeme
POSTGRES_PORT=5433

# ローカル(mvnw)でSpring Bootを起動する時の接続先
# Docker Compose経由で起動する時は docker-compose.yml 側の値が優先される
DB_HOST=localhost
DB_PORT=5433

# JWT(ログイン後の合言葉)を署名するための秘密鍵。本番では別の値に必ず変更すること
JWT_SECRET=changeme
